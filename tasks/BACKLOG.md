# Backlog

Queued work. Items move here → `tasks/CURRENT.md` **Active Task** when started. Backlog lifecycle: `OPEN` → `IN PROGRESS` (tracked in `tasks/CURRENT.md`) → `DONE` (leaves behind a status-board row with final state and evidence).

Nothing here is `IMPLEMENTED`. Backlog items are intentions, not claims about the repository.

## Backlog

| ID | Item | Status | Notes |
|---|---|---|---|
| T-001 | Define the product purpose and scope of `canyou` | DONE | Signed by owner 2026-08-31; recorded verbatim in `docs/PRODUCT.md` (see `tasks/CURRENT.md` → Verified Facts) |
| T-002 | Choose the application stack for the thin slice; record as an architecture decision in `docs/ARCHITECTURE.md` | DONE | Decided in ADR-0001 (Python 3.11+, stdlib-only) per OWNER DIRECTIVE §4-C, 2026-08-31; TS-first rule inapplicable (no existing TS code) |
| T-003 | Enable branch protection on `main` (require PR + passing CI, forbid force pushes) | DONE | Closed 2026-09-01 by the owner in repository settings, then verified externally by the agent: `gh api repos/elazamey/canyou/branches/main` → `"protected": true` (verified 2026-09-01 09:46 UTC) — protection is **ACTIVE** on `main`. Taxonomy, stated so the two words are never confused again: **`DONE`** = the backlog item's work is complete (`OPEN → IN PROGRESS → DONE`); **`ACTIVE`** = the setting remains in force on GitHub — a property of `main`, not a state in `AGENTS.md` §2. Evidence boundary, stated so the row never claims more than its proof: the granular rules (`required_status_checks`, strict up-to-date, admin enforcement, force-push allowance) are admin-readable only — `GET /branches/main/protection*` → `HTTP 403 Resource not accessible by integration` for the agent token — so those individual rules are `NOT VERIFIED (agent scope)` and this row asserts none of them. Corroborating, not proof: every advancement of `main` after `e0b7fcf` is a PR merge commit (`b3b7707` ← `cef93df` ← `6f35c05`, `git log --first-parent main`). No agent touched branch-protection configuration |
| T-004 | Adopt Architecture Decision Records (`docs/decisions/`) | DONE | Mechanism adopted with ADR-0001 (2026-08-31); `verify_adr.sh` enforces form (negative-tested) |
| T-005 | Cut the first release: version policy, tag `CHANGELOG.md` entries | OPEN | The thin slice now exists on `main` (`6f35c05`, 2026-09-01) — the precondition is met; the cut itself remains an owner decision. State stays `OPEN`: the owner's ruling (2026-09-01) is that a satisfied precondition does not become `DONE` unless the taxonomy says so — and it does not; `DONE` marks executed work (version policy + CHANGELOG entry + tag), none of which exists `docs/ROADMAP.md` PHASE 1→2 boundary is the natural place to tag v0.1 |
| T-006 | Decide documentation language policy (English-only vs bilingual) | OPEN | Currently English-only by convention (`CONTRIBUTING.md`) |
| T-007 | Add a markdown lint check to `scripts/verify/` if the docs grow | OPEN | Keep checks offline and deterministic |
| T-008 | Activate CI: get `.github/workflows/ci.yml` onto GitHub, then delete the pending file | DONE | Closed 2026-08-31 with evidence: owner installed the workflow (`09b5090`); first runs failed on SC2164 only; fix `df777a3` (one line); green runs `33445296608`/`33445301389`; pending copy removed (`11178c5`, content-verified identical); post-deletion runs `33445781317`/`33445786957` green — all required jobs PASS (see `tasks/CURRENT.md` → Verified Facts) |
| T-009 | Derive Requirements for the thin slice from `docs/PRODUCT.md` — every requirement carries owner/source | DONE | Signed via OWNER DIRECTIVE Q-7 (2026-08-31) — `docs/REQUIREMENTS.md` is binding; directive addendum (§6/§7/§8/§9) appended with the signature |
| T-010 | ADR-0001: thin-slice architecture — Tool → Policy → Execution → Connector → Provider chain; connectors hold no authority | DONE | ADR-0001 ACCEPTED 2026-08-31 per OWNER DIRECTIVE §4-C (stack + layout + security/traceability shapes); Q-2 concluded |
| T-011 | Thin-slice implementation: Tool Registry + Policy Gate + one Connector | DONE | Closed 2026-09-01 — closure commit `64e5c9488d0a3b7aa768cbf5ad54e81833900fb8` (parent `1a1a878`): COMMITTED / VERIFIED (54 offline tests; `verify_slice.sh`; full suite `PASS — 7 check group(s)`). **DONE ≠ DEPLOYED** — PR #3 **MERGED** 2026-09-01 09:02 UTC: `main` = merge commit `6f35c05ec560e79e0e3f523c0c5b14f11080289f` (checks SUCCESS on the head — runs `33488956138`/`33488974752`; post-merge `main` CI `33490100938` green); Deploy NOT AUTHORIZED. Re-implemented from the documented specification (`docs/REQUIREMENTS.md` + ADR-0001 + directive §4-D/§5) after the original implementation commit (`241fb02f`) was LOST / EVIDENCE UNAVAILABLE — recovery search 2026-09-01 = NOT FOUND (7/7 sources negative); Original SHA preserved: NO. AUTHORIZED by OWNER DIRECTIVE §4-D; §5 exclusions binding |
| T-012 | Security Gate: authentication, authorization, least privilege, secrets handling, connector isolation, policy-bypass resistance, evidence integrity, network failure, timeout handling, error handling | OPEN | **Current priority** after the release chain closes (`docs/ROADMAP.md` §3). Owner-gated: no agent may start it without explicit authorization. Every claim needs a deterministic harness (`scripts/verify/` or `tests/`) — `VERIFIED` means command + output, not opinion |
| T-013 | External validation: prove Registry → Policy → Connector → Transport → Evidence against a real service, not only the offline fake transport | OPEN | `docs/ROADMAP.md` §3. The offline suite stays the gate: real-service evidence is additional, opt-in, network-dependent, and must never place a secret in a file or in evidence output |
| T-014 | Staging environment + smoke tests + Production Readiness checklist | OPEN | `docs/ROADMAP.md` §3 (Security → External → Staging → Smoke → Production Readiness). `T-005` is the natural carrier of the release-side checklist |
| T-015 | Phase 3 packaging: installable CLI + local-first runtime ergonomics for the free tier (incl. local models, free-tier APIs, BYOK) | OPEN | `docs/ROADMAP.md` §4 + §16 PHASE 3. Admission through the pipeline first: requirements rows → ADR → tasks. The signed zero-barrier rule (`docs/CONSTRAINTS.md` 2026-09-01 item 2) binds packaging choices |
| T-016 | Multi-Agent Arena: parallel agents on one task + evidence-based scoring (test pass rate, correctness, regression count, execution time, cost where measurable, reliability, policy compliance) | OPEN | `docs/ROADMAP.md` §5 — the competitive feature. Needs its own ADR (orchestration + scoring); scoring must be recomputable from recorded evidence only. Never "who wrote prettier code" |
| T-017 | Governed self-healing loop: plan → branches → CI → failure analysis → repair → re-test → evidence → owner gate | OPEN | `docs/ROADMAP.md` §6. **Autonomous merge is excluded** by the owner's signed clause; the loop ends at an owner gate, and the Policy Gate stays outside agent control |
| T-018 | Web Arena dashboard (active tasks, agents, branches, PRs, CI, test scores, evidence, policy decisions, execution history, arena comparison) | OPEN | `docs/ROADMAP.md` §7. Read-mostly over repository + CI state; becomes the SaaS entry later and must never be a prerequisite for running locally |
| T-019 | Managed Cloud: hosted execution, dashboard, orchestration, job management, arena results, CI integration, persistent workspace, team collaboration, managed connectors, usage analytics | OPEN | `docs/ROADMAP.md` §9 — first revenue engine. Blocked on real usage/cost data (no pricing before data) and on its own ADR + security review; infrastructure spend before traction is excluded (§17) |
| T-020 | Enterprise controls: RBAC, SSO, advanced audit logs, organization policies, approval workflows, private deployments, advanced secrets management, compliance tooling, centralized governance, dedicated support, custom connectors, SLA | OPEN | `docs/ROADMAP.md` §10. Excluded until real enterprise users exist (§17); the sellable object is Control + Governance + Security + Support, not "AI" |
| T-021 | Agent & Connector Marketplace: publishable agents, connectors, policies, workflows, extensions, with a revenue share | OPEN | `docs/ROADMAP.md` §11. Excluded before a user base exists (§17); needs moderation/licensing/trust policy work that has no requirements row yet |
| T-022 | Commercial artifacts: contribution license / CLA posture, pricing data collection plan, support model, sponsorship channels (GitHub Sponsors, Open Collective, grants, cloud and AI credits) | OPEN | `docs/ROADMAP.md` §8/§12/§13/§15. Any change to `LICENSE` or `CONTRIBUTING.md` is maintainer-owned: an agent may draft it as `DOCUMENTED` only. Grants/credits may not become the financial base (`docs/CONSTRAINTS.md` 2026-09-01 item 8) |
| T-023 | Governance hardening candidates surfaced by the master plan intake — recorded, **not authorized**: (a) register `docs/ROADMAP.md` in `verify_structure.sh` required files; (b) a checker that fails a handoff record whose `Head commit` SHA is unreachable from any remote ref | OPEN | Both touch `scripts/verify/` (the enforcement surface), so they wait for an explicit owner instruction. (b) targets the loss class that erased `241fb02f`, `3bef224`, and `51f4aeb` |

## Ideas parking lot

- Template repository: reuse this baseline as a generator (`canyou init`) — do not start before T-001 is answered.
